# Screen 14 — The ecosystem play · "The architecture is built. The ecosystem is not."

> Inherits `GLOBAL-INSTRUCTIONS.md`. Global rules win unless named-overridden here.
> **Theme: light.** ACT IV. **Density: tight and concrete** — the deliberate counterweight to screen 13's expansiveness.
> Deck is 15 screens; counter reads `14 / 15`.
> Source: `02-NARRATIVE-SPINE.md` screen 15.

## Purpose

Screen 13 made an expansive global claim. This screen answers the question that follows it: *how does this start, and how fast?*

Three moves, ordered by how quickly each can happen: a Bengaluru engineering base (the fix for the location gap diagnosed on screen 11), partnerships with India's software-first robotics companies (frontier perception, without hiring it), and iiQKA.OS opened to Indian developers (the cheapest move with the longest compounding tail).

The screen closes on urgency, delivered as a competitor fact rather than an assertion: FANUC is already executing move three. Architecture without an ecosystem is engineering nobody builds on.

The one idea: *KUKA already has the open platform. What it does not have is anyone in India building on it — and a competitor is moving into that space now.*

## Layout

16:9, light. Three horizontal move-bands stacked vertically, each with a mono index, a title, a short body, and a right-hand "what it unlocks" note. A closing urgency line beneath, set apart. Visual grammar deliberately unlike screen 13's circle — this is linear, sequential, near-term.

```
┌────────────────────────────────────────────────────────────────────────────────┐
│  THE PLAY                                                     [ ⊞ overview ]   │
│  The architecture is built. The ecosystem is not.                              │
│  Three moves, in the order they can happen.                                    │
│                                                                                │
│  ──────────────────────────────────────────────────────────────────────────    │
│  01   BE WHERE THE ENGINEERS ARE                    unlocks ▸ flywheel nodes   │
│       A Bengaluru engineering base — the same                  1 and 4         │
│       Global Capability Centre model hundreds of                               │
│       multinationals already run in India, across                              │
│       automotive, semiconductor, aerospace and software.                       │
│  ──────────────────────────────────────────────────────────────────────────    │
│  02   PARTNER FOR THE BRAIN, NOT AGAINST IT         unlocks ▸ perception       │
│       India's software-first robotics companies are            without hiring  │
│       complementary, not competing. They hold vision            it             │
│       and manipulation intelligence and lack a certified                       │
│       body. KUKA holds exactly that, plus an open OS.                          │
│       Illustrative: CynLr · Ati Motors · Addverb ·                             │
│       Physical Intelligence · Skild · NVIDIA Isaac                             │
│  ──────────────────────────────────────────────────────────────────────────    │
│  03   OPEN iiQKA.OS TO INDIAN DEVELOPERS            unlocks ▸ the longest      │
│       The cheapest move and the one that compounds              compounding    │
│       longest. India's software engineers are the                tail          │
│       largest such population in the world.                                    │
│  ──────────────────────────────────────────────────────────────────────────    │
│                                                                                │
│  FANUC is already making move 04. Architecture without an ecosystem            │
│  is engineering nobody builds on.                                   14 / 15    │
└────────────────────────────────────────────────────────────────────────────────┘
```

- **Top ~14%:** kicker, headline (display), subtitle.
- **Middle ~62%:** three move-bands separated by full-width hairline rules (`#E5E5E8`). Each band: a mono index (`01` / `02` / `03`) in orange at the left, the move title in mono uppercase wide-tracked primary ink beside it, body text beneath (~60% width), and a right-aligned `unlocks ▸` note in mono secondary (~26% width).
- **Bottom ~18%:** the urgency line, display weight, two lines, left-aligned, set apart by additional vertical space.
- **Ratio adaptation:** at 1440×900 reduce band padding ~18% and body type ~6%. The `unlocks ▸` column never wraps below its band; the illustrative partner list may wrap to two lines.

## Steps (reveal order)

**0 — Base:** kicker, headline, subtitle, the mandate block's label and frame, and the four hairline rules with their mono indices and move titles visible; all body text and `unlocks` notes hidden. The four moves are named before any is explained.

**1 — The mandate:** the mandate line `fadeUp`s, then the recruiting line ~220ms later. **This comes first because every move below assumes it.**
**2 — Move 01:** body `fadeUp`, then its `unlocks ▸` note ~200ms later.
**3 — Move 02:** body `fadeUp`, then its `unlocks ▸` note.
**4 — Move 03:** body `fadeUp`, then the illustrative partner list ~250ms later, then its `unlocks ▸` note.
**5 — Move 04:** body `fadeUp`, then its `unlocks ▸` note.
**6 — The urgency:** both closing lines `fadeUp` together, with a beat of separation from step 5.

*(7 beats. Back reverses one at a time. `R` replays from base.)*

## Copy

Exact text. Do not alter.

- **Kicker:** `THE PLAY`
- **Headline:** `The architecture is built. The ecosystem is not.`
- **Subtitle:** `One mandate change, then four moves in the order they can happen.`

**The mandate block** (above the moves, `#F5F5F7` field with a 3px `#FF5800` left edge)
- Label: `THE MANDATE THIS RESTS ON`
- Line (display weight, orange): `From a trade-and-service entity to a Global Capability Centre — R&D, sales and support under one mandate.`
- Sub (body, secondary): `It is also the recruiting pitch. Engineers join a company that builds in India, not one that resells into it — and none of the four moves below survive without the people that changed mandate attracts.`

`trade-and-service` is the term the paradox and diagnosis screens use for the entity's registry classification — keep all three identical.

**Move 01**
- Index: `01`
- Title: `BE WHERE THE ENGINEERS ARE`
- Body: `A Bengaluru engineering base — the same Global Capability Centre model that hundreds of multinationals already run in India across automotive, semiconductor, aerospace and software. India's robotics and AI engineering concentrates there, and KUKA is not in the room.`
- Unlocks: `flywheel nodes 1 and 4`

**Move 02**
- Index: `02`
- Title: `ONBOARD THE WIDER MARKET WITH RaaS`
- Body: `Lead with RaaS rather than capex. It turns a capital decision into an operating line, which opens the MSME and Tier-2/Tier-3 base that cannot buy a cell outright — and every cell placed that way is an instrumented node from the day it is switched on. Revenue while the engineering base is still being built, and the data starts accruing to KUKA at the same time.`
- Unlocks: `revenue now, and the data engine from day one`

**Move 03**
- Index: `03`
- Title: `PARTNER FOR THE BRAIN, NOT AGAINST IT`
- Body: `India's software-first robotics companies are complementary, not competing. They hold vision and manipulation intelligence and lack a certified body, a safety case and an installed base. KUKA holds exactly those, plus an operating system already built as an open platform.`
- Illustrative line: `Illustrative of the category — CynLr · Ati Motors · Addverb · Physical Intelligence · Skild · NVIDIA Isaac`
- Unlocks: `frontier perception without hiring it`

**Move 04**
- Index: `04`
- Title: `OPEN iiQKA.OS TO INDIAN DEVELOPERS`
- Body: `The cheapest of the four moves and the one that compounds longest. iiQKA.OS was rewritten from scratch as a modular open platform. India has the largest population of software engineers in the world. Those two facts have never been put together.`
- Unlocks: `the longest compounding tail`

**Urgency line (display, two lines):**
`FANUC is already making move 04.`
`Architecture without an ecosystem is engineering nobody builds on.`

- **Counter:** `14 / 15`

Move indices are mono, orange, larger than the title. Titles are mono, uppercase, wide tracking, primary ink. Body text is body weight, secondary ink. The illustrative line is mono, small, secondary ink. `unlocks ▸` notes are mono, small, secondary, with the `▸` in orange.

## Data / graphics

No chart, no diagram. The banded structure and typography carry the screen.

- **Move indices:** `#FF5800`, mono, roughly 1.6× the title size. The only large orange elements in the band area.
- **Hairline rules:** 1px `#E5E5E8`, full content width, above each band and below the third.
- **`unlocks ▸` markers:** the `▸` in `#FF5800`, the text in `#6E6E76`.
- **No glow** — light screen.

**`data.js` constants required:**

```js
ECOSYSTEM_PARTNERS = {
  illustrative: ["CynLr","Ati Motors","Addverb",
                 "Physical Intelligence","Skild","NVIDIA Isaac"],
  tier: "frontier",
  note: "ILLUSTRATIVE OF A CATEGORY — not a target list, not a relationship claim",
}
FANUC_OPEN_PLATFORM = { tier:"estimate",
  note:"FANUC's ROS 2 / Python open-architecture posture — verify against FANUC's own published sources before sending; underlying research came partly from social media" }
```

**Evidence chips:** move 02's illustrative list carries `frontier`. The FANUC urgency claim carries `estimate` with its source named. Moves 01 and 03 are proposals rather than factual claims and carry no chips.

## Media slots

None — all native CSS/type. No company logos.

## Animations

- `fadeUp` — body text, illustrative line, `unlocks` notes, urgency lines
- No path drawing, no counting, no circular motion. Deliberately quiet after screen 13.
- **Reduced motion:** all three bands and the urgency line rendered at once. The argument is verbal and loses nothing.
- **Cleanup:** cancel all timers in `onLeave`.

## Acceptance criteria

- Fills the viewport at 1920×1080 and 1440×900 — no scroll, no clipping
- Base state looks finished — three indexed, titled, ruled bands framing the play before any content appears
- **The screen reads as concrete and near-term**, a clear tonal shift from screen 13's expansiveness. If it feels visionary, it has failed its job.
- The three moves read as ordered by feasibility, not by importance
- **The illustrative partner list is unmistakably framed as illustrative** — the phrase `Illustrative of the category` must be present and legible. This is a content rule, not a style preference.
- **No headcount, budget, or timeline figure appears anywhere on this screen**
- The urgency line lands as its own beat and is the most prominent text after the headline
- Only KUKA orange; used on the kicker, the move indices, the `▸` markers and the closing-window statement
- Reduced-motion version complete
- Copy matches this file character for character
- No console errors

## Notes

- **Why the partner list is illustrative and must stay that way.** Naming companies demonstrates command of the ecosystem, which is part of what the author is selling. But this document may circulate, and a named target list invites the question "have you spoken to them?" — which the author cannot yet answer. `Illustrative of the category` does the demonstrative work without the exposure. **Do not remove the framing phrase and do not add companies the author has not reviewed.**
- **Move 01 carries no headcount and no budget deliberately.** This is a screening synopsis; scale and cost belong in the live conversation. The GCC framing sets the scale reference without committing to a number — hundreds of multinationals run substantial engineering centres in India, and the reader supplies the magnitude himself.
- **Move 02's framing is "complementary, not competing."** This matters: several of these companies appeared on screen 06 as clusters in the competitive field. The characterisation must stay consistent — the physical-AI software cluster was described there as `the cluster to partner with, not to race`. This screen is that line paid off.
- **The urgency line is the screen's payload.** It converts the proposition from "worth doing" to "worth doing now", and it does so with a competitor fact rather than an assertion. Do not soften it, and do not move it into the body of move 03 — it needs the separation.
- **Why the mandate block leads.** The moves were originally three, with no statement of the mandate they depend on — which left the screen reading as a list of initiatives rather than a reorganisation. The block states the change once (trade-and-service entity → Global Capability Centre: R&D, sales and support) and carries the talent point with it, because the changed mandate is what makes the company worth joining. It is the prescription for the mandate diagnosed on the diagnosis screen, which deliberately stated the problem without prescribing.
- **Move 02 states RaaS as execution, not as mechanism.** *How* RaaS works — capex into an operating line, fleet ownership making every cell an instrumented node — is explained on the two-layers and flywheel screens. Here it is the commercial move that can start before the engineering base exists: onboard wider, and the data starts accruing from day one. Keep the wording consistent with those two screens and do not re-explain the mechanism.
- **Cross-screen dependencies:**
  - Move 01 is the fix for the location gap diagnosed on **screen 11**. That screen deliberately stated the gap without prescribing; this is where the prescription lands.
  - Move 02 pays off the physical-AI cluster characterisation from **screen 06**.
  - `iiQKA.OS ... rewritten from scratch as a modular open platform` must match the wording used on **screens 05 and 07** exactly.
  - The flywheel-node reference in move 01's `unlocks` note points back to **screen 13**. Keep node numbering consistent.
- **Verification flags** (carry into `VERIFY-BEFORE-SENDING.md`): FANUC's ROS 2 / Python open-architecture posture is the load-bearing claim in the urgency line and came partly from social-media sources. Confirm against FANUC's own published material before sending, or restate at the level that is solid: that FANUC has opened its architecture and is pushing physical-AI capability toward emerging-market SMEs. Confirm every company in the illustrative list is still active and not recently acquired — the same flag as screen 06.
