/**
 * pages/02a-act-i.js — Screen 03 · ACT I divider (light). act-divider.js.
 *
 * Filename numbering: the four dividers were added after the fifteen content
 * screens were built, so each takes the number of the screen it follows plus
 * a letter (`02a` runs after `02-four-acts`). Renumbering twelve files and
 * their ids to close the gap would touch every screen in the deck for no
 * behavioural gain — running order comes from the script order in index.html,
 * and the counter and overview grid derive from the registered page count.
 *
 * This one sits after the cover and the four-moves roadmap, so the act it
 * opens is already familiar: the roadmap named all four, this names the one
 * about to start.
 */
window.ActDivider.register("act-1");
