# Page 09 — Superhuman · "The buckets select for it — the bonus, and the first beachhead"

> Inherits `GLOBAL-INSTRUCTIONS.md`. Light theme. Act II close. Locator: `locatorMap("superhuman")`.

## Purpose

Show that superhuman capability is a structural consequence of building the three buckets well — not a separate bet — and name it as the first beachhead, while honestly flagging the vertical-depth tension.

The one idea: *A middle built for the general case is automatically the only thing that can do superhuman work — so it's a free market, and our first proof.*

## Layout

Comparison archetype: two columns. Left = SUBSTITUTION (folding, vacuuming, bin-picking) — cost-arbitrage, hardware/China wins, commoditizes → down to the full-stack players. Right = SUPERHUMAN (mining, oil & gas, nuclear, subsea, extreme weather) — no data, high WTP, needs all three buckets. Below the right column, a 3-row mapping: no-data→Adaptation, goal-driven→Autonomy, high-consequence→Assurance. A single honest caption flags the beachhead-vs-platform tension. Locator top-right. Carry bottom.

## Steps (reveal order)

**0 — Base:** kicker, title, locator, the two empty column headers.
**1 — Substitution:** left column fills; the "race to the bottom" line.
**2 — Superhuman:** right column fills; the "race to the top" line.
**3 — The mapping:** the three property→bucket rows `drawPath`/`fadeUp` — showing the buckets select for superhuman.
**4 — Beachhead + tension:** the beachhead line + the honest tension caption; carry.

*(4 beats.)*

## Copy

- **Kicker:** `The bonus that falls out`
- **Title:** **We don't chase substitution. We unlock the impossible.**
- **Substitution (left):** Folding, vacuuming, bin-picking — a race to the bottom on cost, where hardware and supply-chain scale win and margins vanish. Leave it to the full-stack players.
- **Superhuman (right):** Mining, oil & gas, nuclear, subsea, extreme weather — no human demonstration data, high willingness-to-pay, no incumbent data lead.
- **Mapping:** `no data → Adaptation` · `goal-driven, no recipe → Autonomy` · `high consequence → Assurance`. **A middle built for the general case is the only thing that can do this.**
- **Beachhead + tension:** Superhuman is the story's bonus and the roadmap's first proof. *Honest note:* these verticals demand deep domain + hardware specialization — so we go vertical-deep on one beachhead first, then generalize the platform.
- **Carry:** `the buckets select for superhuman — and against commodity substitution`

## Data / graphics

Native two-column comparison. Left column muted (secondary ink); right column ink + bronze/slate accents on the mapping. `tierChip` not required here. The tension note in secondary ink (honest, not hidden).

## Media slots

None.

## Animations

`fadeUp` columns/rows; `drawPath` the mapping connectors. **Reduced motion:** both columns + mapping + note shown.

## Acceptance criteria

- The property→bucket mapping is explicit and correct.
- The vertical-depth tension is stated openly (weak joint #5), not buried.
- Left column visibly de-emphasised vs right.

## Notes

- This is the emotional peak of Act II — the founder should feel the market open up. Keep it confident, not hypey.
