# Page 18 — Beyond a single robot · "The embodiment becomes the environment"

> Inherits `GLOBAL-INSTRUCTIONS.md`. Light theme. Act V opener. Locator: expand from `locatorMap()`.

## Purpose

Expand the vision at the end: as the ecosystem matures, "embodiment" stops meaning a single robot and becomes whole intelligent environments; the Composer extends to compose them. Show the thesis scales far past robots.

The one idea: *The same middle that composes a robot composes an intelligent warehouse, a self-driving lab, a fleet.*

## Layout

Callback: bring the slide-01 locator map to centre, then let its right-hand "embodiment world" node **expand** into a small constellation of environment icons/labels — intelligent warehouse, self-driving lab, orchestrated fleet, IoT plant — all still bridged by the same ink Composer. Kicker/title top-left. Carry bottom.

## Steps (reveal order)

**0 — Base:** kicker, title, the familiar bridge map (from page 01) at rest, centre.
**1 — The shift:** the "embodiment world" label morphs to "embodied *systems*"; the single-body nodes expand outward (`radiate`) into environment nodes.
**2 — Compose the environment:** the Composer keystone re-emphasises; a line notes it now composes whole systems.
**3 — The line:** carry.

*(3 beats.)*

## Copy

- **Kicker:** `The horizon`
- **Title:** **Physical AI is bigger than a robot.**
- **The shift:** The embodiment world becomes embodied *systems* — intelligent warehouses where gates, valves, conveyors and arms co-orchestrate to a goal; self-driving labs where instruments pursue an objective; orchestrated fleets.
- **Compose the environment:** The same three buckets and the same Composer extend from one body to a whole environment — the middle, at system scale.
- **Carry:** `we compose robots today — and intelligent environments tomorrow`

## Data / graphics

Native. Reuse `locatorMap()` geometry; the embodiment cluster expands into environment nodes (bronze). Composer stays ink. No new colours.

## Animations

`morphSwap` the embodiment label; `radiate` the environment nodes; re-emphasise the keystone. **Reduced motion:** expanded map with environment nodes shown.

## Acceptance criteria

- Clear callback to page 01 (same map), now expanded — the audience recognises it.
- The Composer visibly extends to environments.

## Notes

- This is the penultimate emotional lift before the close — keep it clean, let the callback do the work.
