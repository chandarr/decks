# Page 03 — How the LLM era spanned out · "Value went to the ends; the neutral integrator won the middle"

> Inherits `GLOBAL-INSTRUCTIONS.md`. Light theme. Act I. One slide only (per spine).

## Purpose

Earn the right to predict. Show the LLM era as a five-beat arc whose pattern we then map onto Physical AI. This is the evidence page — factual, not aspirational — and it demonstrates AC's framing ability to a market-watcher founder.

The one idea: *In the LLM era, value bifurcated to the ends, the middle of each layer commoditized, and the neutral integrator captured the gap — a pattern that repeats.*

## Layout

A horizontal timeline spanning the stage at mid-height, five evenly-spaced ink nodes on a hairline axis, each with a short label above and a one-line note below. A mono kicker + title top-left. A carried lesson line at the bottom. The fifth node carries a small `--alert`-tinted footnote marker for the Scale/Meta cautionary tale.

## Steps (reveal order)

**0 — Base:** kicker, title, the empty axis with five tick marks.
**1–5:** each beat reveals left→right (`fadeUp` node + note; the axis `drawPath` extends to it). Beat 3 rolls the `~1,500×` figure with `countUp`. Beat 5 reveals the neutrality footnote.
**6 — Lesson:** the carry line.

*(6 beats. Back reverses one node at a time.)*

## Copy

- **Kicker:** `Precedent — the LLM era`
- **Title:** **We've already watched this span out.**
- **Beat 1 — Research era:** Scaling laws; intelligence as a research curiosity.
- **Beat 2 — The land-grab:** ChatGPT; capital floods the frontier; a compute arms race.
- **Beat 3 — Commoditization:** Open weights reset price/performance; the cost of intelligence falls **~1,500×** in six years.
- **Beat 4 — Value migrates:** To the two ends — compute below, apps + services above — while each layer's undifferentiated middle compresses.
- **Beat 5 — The neutral integrator wins:** Adaptation + orchestration capture the gap. *Footnote:* Meta's stake in Scale AI made it "captured"; frontier labs fled to neutral rivals overnight.
- **Carry:** `Value went to the ends. The scarce, neutral integrator won the middle.`

## Data / graphics

Native. The `1,500×` as a `countUp` tabular figure (ink). Footnote marker in `--alert`. No chart beyond the timeline axis.

## Media slots

None.

## Animations

`drawPath` axis extension; `fadeUp` nodes/notes; `countUp` the multiple. **Reduced motion:** full timeline + final figure shown.

## Acceptance criteria

- Reads as factual precedent, not opinion.
- Five beats, correct order; the `1,500×` lands on beat 3.
- Neutrality footnote present and tied to beat 5.

## Notes

- Keep it to one slide (spine decision). Deeper LLM value-chain map lives in the optional appendix.
