# Page 08 — The Composer · "Requirement + budget + application → the tailored stack"

> Inherits `GLOBAL-INSTRUCTIONS.md`. Light theme. Act II keystone. Locator: `locatorMap("composer")`.

## Purpose

The emergent keystone: the product the three buckets compose into. Show it as a console that takes constraints in and returns a complete, tailored, certified stack — emergent (not buildable-first), neutral by construction, and the services→platform valuation re-rate.

The one idea: *When the three buckets are modular and instrumented, a system can generate the whole stack on demand.*

## Layout

Instrument / console archetype. Left: an INPUTS panel — three labelled fields `requirement`, `budget`, `application`. Centre: the ENGINE — a compact rendering of the three buckets (Adaptation/Autonomy/Assurance) as an ink processing core. Right: the OUTPUT — a "stack" card listing the composed result (model · adaptation recipe · embodiment · harness · assurance path). Below: a small v1→vN maturity strip. Locator top-right. Carry bottom.

## Steps (reveal order)

**0 — Base:** kicker, title, locator, the empty console (inputs blank, engine dim, output card empty).
**1 — Inputs:** the three input fields populate with an illustrative example (clearly marked *example*).
**2 — Compose:** the engine (three buckets) lights in ink; a `converge` motion from inputs into the core.
**3 — Output:** the stack card fills line by line (`fadeUp` staggered).
**4 — Maturity + neutrality:** the v1→vN strip and the two framing lines (emergent; neutral by construction; the re-rate).

*(4 beats.)*

## Copy

- **Kicker:** `The keystone`
- **Title:** **The Composer.**
- **Inputs (example, marked illustrative):** `requirement: inspect + seal welds, confined space` · `budget: sub-$40k/unit, offline-capable` · `application: subsea pipeline`
- **Output stack (illustrative):** `model: open-weight VLA, distilled` · `adaptation: task-tuned, INT4, latent-perception` · `embodiment: tethered crawler (vendor-neutral)` · `harness: reflex+reactive on-edge, cloud deliberative` · `assurance: failure-set + safety case → cert path`
- **Framing:**
  - **Emergent, not buildable-first** — v1 is a human-in-the-loop config tool over a library of validated recipes; autonomy grows with the recipe library + deployment data.
  - **Neutral by construction** — only an entity uncaptured on both ends can compose across models and bodies. Vendor-agnosticism becomes the enabling condition, not a constraint.
  - **The re-rate** — this is where a research + services lab becomes a platform.
- **Carry:** `the three buckets are the parts; the Composer is the product`

## Data / graphics

Native. Inputs/engine/output as `.panel`s connected by ink flow. Illustrative example values MUST be labelled *example* (mono, secondary). `tierChip("BUILDING")` on v1; `tierChip("ASPIRATIONAL")` on vN autonomy.

## Media slots

None.

## Animations

`fadeUp` fields/lines; `converge` inputs→engine; `scaleIn` output card; `crossFade` the v1→vN strip. **Reduced motion:** populated console + output shown, example labelled.

## Acceptance criteria

- The example is unmistakably marked illustrative (defuses "magic" objection).
- The v1(human-in-loop)→vN(autonomous) path is explicit — the incremental credibility.
- Neutrality-as-enabler stated.

## Notes

- Weak joint #4 (Composer = magic) is defused by the v1 recipe-library framing and the labelled example — keep both.
