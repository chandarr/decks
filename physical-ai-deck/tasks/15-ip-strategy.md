# Page 15 — IP strategy · "Open what recruits, protect what compounds"

> Inherits `GLOBAL-INSTRUCTIONS.md` (§11 center-weight; §12 motion where it adds value; §13 reveal one-at-a-time). Light theme, off-white/ink, no glow. Act IV. Deck v3.

## Purpose

Show a deliberate IP posture that balances research credibility (which recruits) with defensibility (which compounds): publish the science, patent the methods, keep the crown-jewel data, and shape the certification standard as durable position. A clean, confident slide.

The one idea: *Publish the science, patent the methods, keep the data — and set the standard.*

## Layout (center-weighted, three columns — §11)

All content in the central band (y ≈ 210–770); bottom ~20% chrome only.

- **Top chrome:** kicker top-left; slide-no "15" top-right.
- **Title** — Archivo, ~y160–240.
- **Three columns** centered (≈ y320–620): `PUBLISH` · `PATENT` · `KEEP`, each = an ink icon + a bold header + one line of "what" + a one-line rationale. The `KEEP` column is subtly emphasized (the crown jewels).
- **Standards-as-position line** below the columns (within band, ~y660).
- **Takeaway** centered ~y730.

## Steps (reveal order — §13, one at a time)

**0 — Base:** kicker, slide-no, title, three empty column headers (`PUBLISH · PATENT · KEEP`) faint.
**1 — Publish:** the Publish column fills.
**2 — Patent:** the Patent column fills.
**3 — Keep:** the Keep column fills, emphasized (a subtle lock/vault close — the crown jewels).
**4 — Standard + line:** the standards-as-position line, then the takeaway.

*(4 advance beats after base. Back reverses; no persistent loops.)*

## Copy (exact)

- **Kicker:** `How we build · what we own`
- **Title:** **Publish the science. Patent the methods. Keep the data.**
- **PUBLISH** — research on embodied assurance, world-model compression, adaptation, evaluation. *Attracts the best people and sets the field's terms.*
- **PATENT** — model→embodiment adaptation, edge-orchestration, and safety-case techniques. *Defensible and licensable.*
- **KEEP (trade secret)** — the failure-data corpus, the orchestration recipes, deployment data. *The crown jewels — never published, never patented.*
- **Standards-as-position:** Shaping the certification standard is a seat at the head of the table — not a filing.
- **Takeaway (central band):** **Open what recruits. Protect what compounds.**

*(Sentence case for prose; `PUBLISH · PATENT · KEEP` UPPER/mono headers; column rationales italic `--secondary`.)*

## Data / graphics

Native inline SVG. Three columns as light hairline panels of equal size/padding; the `KEEP` panel slightly heavier border / a faint fill to read "vault." Ink icons: Publish = an open book / outward radiate; Patent = a shield; Keep = a lock/vault. Asset items as short mono chips inside each column. No glow; no `--alert`. Keep the three columns balanced and legible from the back.

## Media slots

None — all native.

## Animations

`fadeUp` columns (one at a time — §13); a subtle `scaleIn`/lock-close on the `KEEP` icon at beat 3 (the one bit of motion that adds meaning); `fadeUp` the standards line + takeaway. **Reduced motion:** all three columns + standards line + takeaway shown at rest, `KEEP` in its closed/emphasized state. **Cleanup:** trivial.

## Acceptance criteria

- **§13:** three columns revealed one at a time; `KEEP` visibly the emphasized crown-jewel column.
- **Center-weighted:** columns, standards line, takeaway within the central band (§11).
- The failure-data corpus + orchestration recipes are unambiguously trade-secret (ties to slides 06 & 09); "standards as position" present.
- Off-white/ink only, no glow, no `--alert`; base finished; reduced-motion correct; no console errors.

## Notes

- Continuity: KEEP = the moat's data (failure-data from slide 09, orchestration recipes from slide 06). Publish feeds the talent engine (slide 13).
- Easy slide — keep motion minimal (only the KEEP emphasis). Don't over-design.
