# Page 19 — Closing page · "The middle is open"

> Inherits `GLOBAL-INSTRUCTIONS.md` (§11 center-weight). Light theme, off-white/ink, no glow. Act V close. Deck v3.
> Minimal closing page — the elaborate "how we'd work / what I'd own / first moves" is intentionally omitted (implied by the deck). This is a dignified sign-off that lands the thesis. The ask is implied, never stated.

## Purpose

End on a quiet, confident final page: restate the thesis, call back to the opening spine, and let the room leave on the north star. No CTA, no contact, no "hire me."

The one idea: *The biggest seat in physical AI is open — and we know how to build it.*

## Layout (center-weighted — §11)

Spacious and calm; everything in the central band.

- **Top chrome:** slide-no "19" top-right, faint. No busy kicker.
- **Faint spine callback (background):** the slide-01 spine `MODELS ——●—— MACHINES` centered, at low contrast (≤10% ink) — a quiet echo of the opener, the `●` (us) the only slightly-emphasized element.
- **Final punch** (Archivo, large) centered over the spine.
- **One mission line** beneath it (`--secondary`).
- **A quiet closer line** below that.

## Steps (reveal order)

**0 — Base:** the faint spine + the mission line, at rest.
**1 — The punch:** the final punch `fadeUp`s in and holds.
**2 — The closer:** the quiet closer line fades in.

*(2 advance beats after base. Minimal — this is a closer, not a content slide.)*

## Copy (exact)

- **Final punch:** **The middle is open.**
- **Mission line:** The orchestration layer between the world's models and its machines — edge, competence, confidence.
- **Closer line:** *And we know how to build it.*

*(Sentence case; spine labels `MODELS` · `MACHINES` UPPER/mono, faint.)*

## Data / graphics

Native inline SVG. Reuse the slide-01 spine geometry at ≤10% ink for the callback; the `●` orchestration node slightly stronger than the rest but still quiet. No panels, no glow, no `--alert`. Generous whitespace — this page is mostly air.

## Media slots

None — all native.

## Animations

`fadeUp` the punch then the closer; the spine is static (present from base). **Reduced motion:** whole page at rest — spine, punch, mission line, closer all visible. **Cleanup:** trivial.

## Acceptance criteria

- **Center-weighted, spacious:** punch + lines centered; the spine callback readable but faint; nothing near the bottom edge (§11).
- No CTA / no "hire me" / no contact / no first-moves list — the ask is implied only.
- Clear callback to slide 01 (the spine). Off-white/ink, no glow; reduced-motion correct; no console errors.

## Notes

- Register matters most here: confident, unhurried, quiet. This is the note the founder leaves the room on — restraint reads as strength.
- This closes the deck at 19 pages (18 content + this closing page).
